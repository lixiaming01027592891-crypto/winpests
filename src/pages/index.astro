---
import { Picture } from "astro:assets";
import BaseLayout from "../layouts/BaseLayout.astro";
import { site, services, process, cases, faqs } from "../data/site";
import { photos, type PhotoKey } from "../data/media";

const featured = cases.slice(0, 6);
---

<BaseLayout>
  <section class="hero">
    <div class="hero__media">
      <Picture
        src={photos.hero}
        formats={["avif", "webp"]}
        alt="防治人員在工地進行病媒處理"
        loading="eager"
        fetchpriority="high"
        widths={[720, 1080, 1600, 2400]}
        sizes="100vw"
      />
      <span class="image-note">圖像 · 情境示意</span>
    </div>
    <div class="hero__copy">
      <p class="eyebrow">新北板橋 · {site.founded} 年設立</p>
      <h1>家裡有蟲，<br />先看它從哪裡來。</h1>
      <p class="hero__lead">
        白蟻、老鼠、蟑螂，或工地開始積水生蚊。打個電話說一下現場，我們再跟你約時間。
      </p>
      <div class="hero__cta">
        <a class="btn btn-solid" href={site.phoneHref}>免付費 {site.phoneDisplay}</a>
        <a class="btn" href={site.lineUrl} target="_blank" rel="noreferrer">用 LINE 說現場</a>
      </div>
      <ul class="hero__proof" aria-label="公司登記與服務資訊">
        <li><b>{site.founded}</b><span>板橋設立</span></li>
        <li><b>{site.taxId}</b><span>企業社統編</span></li>
        <li><b>病媒防治</b><span>登記營業項目</span></li>
        <li><b>合法用藥</b><span>現場可看標示</span></li>
      </ul>
    </div>
  </section>

  <section class="section" id="services">
    <div class="wrap">
      <div class="section-head">
        <div>
          <p class="eyebrow">服務</p>
          <h2>常見的四種情況</h2>
        </div>
        <p class="mute">
          作法不一樣。白蟻看木頭，老鼠看通道，店面看打烊以後，工地看積水。
        </p>
      </div>
      <div class="service-grid">
        {
          services.map((item) => {
            const img = photos[item.image as PhotoKey];
            return (
              <a class="service-card" href={`/services/${item.slug}/`}>
                <div class="service-card__media">
                  <Picture src={img} formats={["avif", "webp"]} alt={`${item.title}的作業畫面`} widths={[640, 960, 1400]} sizes="(min-width: 980px) 50vw, 100vw" />
                </div>
                <div class="service-card__body">
                  <span class="image-note">圖像 · 情境示意</span>
                  <p class="eyebrow">{item.kicker}</p>
                  <h3>{item.title}</h3>
                  <p>{item.lead}</p>
                  <span class="btn">看這項怎麼做</span>
                </div>
              </a>
            );
          })
        }
      </div>
    </div>
  </section>

  <section class="section section-light" id="process">
    <div class="wrap">
      <div class="section-head">
        <div>
          <p class="eyebrow">怎麼配合</p>
          <h2>先看過，才報價</h2>
        </div>
        <p class="mute">白蟻和老鼠尤其是這樣。電話裡能講清楚的，我們也會直說。</p>
      </div>
      <div class="process">
        {
          process.map((step) => (
            <article>
              <b>{step.n}</b>
              <h3>{step.title}</h3>
              <p>{step.text}</p>
            </article>
          ))
        }
      </div>
    </div>
  </section>

  <section class="section" id="cases">
    <div class="wrap">
      <div class="section-head">
        <div>
          <p class="eyebrow">作業情境</p>
          <h2>常見的作業場域</h2>
        </div>
        <p class="mute">工地、透天、社區地下室、餐飲後場、學校走廊。圖片為情境示意，實際作法依現場勘查。</p>
      </div>
      <div class="case-grid">
        {
          featured.map((item) => {
            const img = photos[item.image as PhotoKey];
            return (
              <a class="case-card" href={`/cases/#${item.slug}`}>
                <Picture src={img} formats={["avif", "webp"]} alt={`${item.place}｜${item.title}`} widths={[640, 1200, 1800]} sizes="50vw" />
                <figcaption>
                  <span class="image-note">圖像 · 情境示意</span>
                  <span>{item.place} · {item.service}</span>
                  <h3>{item.title}</h3>
                  <p class="case-card__cue">先看：{item.signal}</p>
                </figcaption>
              </a>
            );
          })
        }
      </div>
    </div>
  </section>

  <section class="section section-light">
    <div class="wrap split">
      <figure>
        <Picture src={photos.equipment} formats={["avif", "webp"]} alt="防治機具與餌站" widths={[800, 1200]} sizes="50vw" />
        <figcaption class="image-note">圖像 · 情境示意</figcaption>
      </figure>
      <div>
        <p class="eyebrow">登記資料</p>
        <h2>用藥合法，現場可以給你看</h2>
        <p class="mute">
          文盈蟲害防治企業社，負責人{site.owner}，統編 {site.taxId}。做病媒防治和環境用藥零售。施工時你可以要求看出執照和藥名。
        </p>
        <ul class="evidence-list">
          <li><b>01</b><span><strong>公司資料可核對</strong>企業社名稱、負責人與統編直接列在網站。</span></li>
          <li><b>02</b><span><strong>施工前講清楚</strong>範圍、用藥、小孩寵物避開方式與保固內容先確認。</span></li>
          <li><b>03</b><span><strong>現場標示可查看</strong>需要時可要求查看執照、藥名與施工注意事項。</span></li>
        </ul>
        <p>
          <a class="btn btn-dark" href="/about/">關於這間社</a>
        </p>
      </div>
    </div>
  </section>

  <section class="section section-light" style="padding-top:0">
    <div class="wrap faq">
      <p class="eyebrow">常見問題</p>
      <h2>常被問到的</h2>
      {
        faqs.map((item) => (
          <details>
            <summary>{item.q}</summary>
            <p>{item.a}</p>
          </details>
        ))
      }
    </div>
  </section>
</BaseLayout>
